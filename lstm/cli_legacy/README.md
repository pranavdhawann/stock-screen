Legacy LSTM CLI
===============

This standalone plotting CLI is unsupported and kept only for historical local experiments. The web app uses `app/services/forecasting.py` with the bundled `lstm/model.pt` checkpoint and does not load this CLI config.
